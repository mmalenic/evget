# libevgetwindows

The Windows backend for evget. It captures input events through the Win32 API and converts
them into the platform independent schema.

This is built as the `libevgetwindows` target and depends on `libevget`. See the
[top-level README](../README.md) for build and usage instructions.
