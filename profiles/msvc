# Windows MSVC profile.

include(./base)

{% set compiler = os.getenv("EVGET_COMPILER") or detect_api.detect_msvc_compiler() %}
{% set version = os.getenv("EVGET_VERSION") or compiler[1].major %}

[settings]
compiler={{ compiler[0] }}
compiler.version={{ version }}
compiler.cppstd=23
compiler.runtime=dynamic
