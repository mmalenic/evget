# libevgetx11

The X11 backend for evget. It captures input events through the X11 XInput extension and
converts them into the platform independent schema.

This is built as the `libevgetx11` target and depends on `libevget`. See the
[top-level README](../README.md) for build and usage instructions.
